---
export interface Props {
    tag?: 'h2' | 'h3';
    id?: string;
}

const { tag = 'h2', id } = Astro.props;
---

<div id={id} class="divider divider-primary gap-xl mt-10 mb-10 lg:mb-16 max-w-full h-auto">
    {
        tag === 'h2' && (
            <h2 class="font-serif text-3xl lg:text-6xl" data-aos="fade-up">
                <slot />
            </h2>
        )
    }
    {
        tag === 'h3' && (
            <h3 class="font-serif text-3xl lg:text-4xl" data-aos="fade-up">
                <slot />
            </h3>
        )
    }
</div>
